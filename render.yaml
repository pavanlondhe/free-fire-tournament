services:
  - type: web
    name: ff-arena
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 24
      - key: ADMIN_PASSWORD
        sync: false